<div class="container mt-4">
    <h2>Listado de Marcas</h2>
    <a href="/dashboard/brands/new" class="btn btn-success">Crear Marca</a>
    <table class="table table-bordered table-striped text-center mt-4">
        <thead>
            <tr>                
                <th>Marca</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <% brands.forEach(brand=> { %>
                <tr>                    
                    <td>
                        <%= brand.marca %>
                    </td>
                    <td>
                        <a href="/dashboard/brands/<%= brand.id %>/edit" class="btn btn-sm btn-primary">Editar</a>
                        <form action="/dashboard/brands/<%= brand.id %>/delete" method="POST" class="d-inline">                            
                            <button type="submit" class="btn btn-sm btn-danger">Eliminar</button>
                        </form>                        
                    </td>
                </tr>
                <% }); %>
        </tbody>
    </table>
</div>